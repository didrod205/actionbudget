# actionbudget report

**82/100 (B)** across **2** workflow(s) — 0 error(s), 6 warning(s), 6 info(s)

## examples/.github/workflows/lean.yml — Lean CI — 100/100 (A)

✓ No waste found.

## examples/.github/workflows/wasteful.yml — Wasteful CI — 64/100 (D)

| | Rule | Where | Issue | Fix |
| - | ---- | ----- | ----- | --- |
| ⚠ | `no-concurrency` | L4 | No concurrency group — superseded runs keep running | Add: |
| ⚠ | `missing-cache` | job `test` L20 | setup-node without dependency caching | Enable caching, e.g. add `with: { cache: 'npm' }` to the setup-node step. |
| ⚠ | `missing-cache` | job `mac-build` L30 | setup-node without dependency caching | Enable caching, e.g. add `with: { cache: 'npm' }` to the setup-node step. |
| ⚠ | `duplicate-triggers` | L4 | push + pull_request run CI twice per PR commit | Limit push to your main branches: |
| ⚠ | `large-matrix` | job `test` L10 | Matrix expands to 12 jobs | Trim the matrix to the combinations you actually ship, or use `include` for the few extras. |
| ⚠ | `frequent-schedule` | L6 | Schedule runs every ~5 min | Run less often (≥ every 15 min) unless the frequency is truly required. |
| ℹ | `costly-runner` | job `mac-build` L26 | macOS runner bills at 10× Linux | Run on ubuntu-latest where possible, and reserve macOS for the few jobs that need it. |
| ℹ | `no-timeout` | job `test` L10 | Job has no timeout-minutes | Add `timeout-minutes: 15` (or a sensible cap) to the job. |
| ℹ | `no-timeout` | job `mac-build` L26 | Job has no timeout-minutes | Add `timeout-minutes: 15` (or a sensible cap) to the job. |
| ℹ | `no-path-filter` | L4 | `push` has no paths filter | Add `paths:` (or `paths-ignore:`) so unrelated changes skip CI. |
| ℹ | `no-path-filter` | L5 | `pull_request` has no paths filter | Add `paths:` (or `paths-ignore:`) so unrelated changes skip CI. |
| ℹ | `full-history-checkout` | job `test` L17 | checkout fetches full git history (fetch-depth: 0) | Drop `fetch-depth: 0` unless a step truly needs full history. |

---
<sub>Generated by actionbudget v0.1.0 · 2026-06-04T06:23:23.712Z</sub>
